from imagescripter import *
def Ping_Check_NVR_(ip):
    from pythonping import ping
    x = str(ping(ip, verbose=True))
    if 'Request timed out' in x:
        raise ValueError('NVR might be down,its not pinging back')
    else:
        return True
	
Ping('192.168.1.169')