import tkinter as tk

class Start_End_Window_Self_Destruct_Class():
    def __init__(self):
        import tkinter as tk
        root = tk.Tk()
        root.attributes('-topmost', 'true')
        root.attributes("-fullscreen", True)
        root.configure(background='white')
        #root.after(10000, lambda: root.destroy())  # Destroy the widget after 20 seconds
        root.after(250, lambda: root.destroy())
        #L = tk.Label(root, text=Text)
        #L.pack()
        root.mainloop()

if __name__ == "__main__":
    Start_End_Window = Start_End_Window_Self_Destruct_Class()

