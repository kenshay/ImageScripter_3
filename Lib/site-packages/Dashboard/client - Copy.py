#!/usr/bin/env python3
import time


import socket


HOST = '172.16.42.56'
#HOST = '127.0.0.1'
PORT = 65432

with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
    s.connect((HOST, PORT))
    #message = ''
    #message.join(format(ord(x), 'b') for x in 'You Suck ' + str(i))
    for i in range(1000):
        Byte_Message = str.encode('you are cool ' + str(i))
        s.sendall(Byte_Message)
        data = s.recv(1024)


print('Received', repr(data))
#time.sleep(10)
